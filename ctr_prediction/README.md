This folder contains 2 files:
- main.ipynb: This notebook contains all the code needed for preprocessing and models training, simply open and run the whole preprocess cells, then run each model training cell and observe the result.
Warning: as the dataset is large (about 20 million rows, training some models might runs into out-of-memory error), ensure you have enough resources before training the model.
- main.css: This css version of the notebook contains the training and evaluation of the models I ran, simply run the code on any online CSS editor and observe the results.
